name: CD

on:
  workflow_dispatch:
  workflow_run:
    workflows: ['dockerimage']
    types: [completed]

jobs:
  second:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Second action successfully finished!"
